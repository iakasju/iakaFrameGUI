{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities minimales de la fenetre principale (P1).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
