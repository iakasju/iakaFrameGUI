# Emplacement réservé — sidecar CLI `@naonedge/iakaframe` (P3, NON branché en P1)

Ce dossier est l'**emplacement prévu** pour le binaire du **CLI en sidecar** qui servira au
**déploiement (P3)** : génération/pose des kits `.claude/` depuis une team PURE.

> **P1 : rien ici.** Le CLI n'est **pas** invoqué à ce stade (AR-6). On documente seulement
> l'emplacement (convention Tauri `externalBin`/sidecars = `src-tauri/binaries/`). Aucune
> entrée `bundle.externalBin` n'est ajoutée à `tauri.conf.json` tant que P3 n'est pas ouvert.

Réf. : `specs/instructions/P1-coquille-forge-authoring.md` § 2.2 et § 5.6 ;
`specs/PROJET.md` (moteur méthode = CLI `@naonedge/iakaframe` en sidecar).
